package telran.strings;

public class Validations {
	public static boolean isArithmeticExpression(String expression) {
		//TODO
		//1. Check against the regular expression
		//2. Checking brackets pairness For each '(' there should be ')'
		//consider introducing counter that increased for '(' and decreased for ')'
		//if during passing over the string counter < 0 returning 	
		//if after passing whole string !=0 returning false
		return false;
	}
}
